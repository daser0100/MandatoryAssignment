@model IEnumerable<IT.Models.About>

<h2>@ViewData["DisplayedTitle"]</h2>
<h3>@ViewData["Message"]</h3>

@foreach (var item in Model)
{
    <p>@Html.DisplayFor(modelItem => item.Section)</p>
}

